#include <iostream>
#include <string>
using namespace std;

// =================================


int main(){
    // initialze pointer as uninitialze to not take garbge value
    int *p = NULL;
    // = 0 only zero not any number can be accept 

    cout << p;
    return 0;
}